openapi: 3.2.0
info:
  title: GZW Data Keys API
  version: 4.0.0
  description: Comprehensive Gray Zone Warfare game data API.
servers:
- url: https://gzw-data.dev
tags:
- name: Keys
paths:
  /api/keys:
    get:
      summary: keys (126 items)
      tags:
      - Keys
  /api/keys/{id}:
    get:
      summary: Get one keys record by id
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      tags:
      - Keys
  /api/v1/keys:
    get:
      summary: keys (126 items)
      tags:
      - Keys
  /api/v1/keys/{id}:
    get:
      summary: Get one keys record by id
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      tags:
      - Keys
